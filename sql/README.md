# دليل قاعدة البيانات
# Database Guide

## نظرة عامة
يحتوي هذا المجلد على ملفات SQL اللازمة لإعداد قاعدة البيانات للنظام.

## الملفات

### 1. schema.sql
يحتوي على:
- تعريف جميع الجداول
- العلاقات بين الجداول (Foreign Keys)
- البيانات الأساسية (Roles, Status, Priorities)
- المستخدم الافتراضي (admin)
- الفهارس (Indexes) لتحسين الأداء

### 2. seeds.sql
يحتوي على بيانات أولية للاختبار والتطوير:
- 12 مستخدم (3 مديرين + 9 أعضاء)
- 6 مشاريع بحالات مختلفة
- أعضاء لكل مشروع
- 30+ مهمة موزعة على المشاريع
- إشعارات
- سجل أنشطة

## خطوات التثبيت

### الطريقة الأولى: من سطر الأوامر

```bash
# 1. إنشاء قاعدة البيانات
psql -U postgres
CREATE DATABASE project_management_db;
\q

# 2. تشغيل ملف المخطط
psql -U postgres -d project_management_db -f schema.sql

# 3. تشغيل ملف البيانات الأولية (اختياري)
psql -U postgres -d project_management_db -f seeds.sql
```

### الطريقة الثانية: من pgAdmin

1. افتح pgAdmin
2. أنشئ قاعدة بيانات جديدة باسم `project_management_db`
3. انقر بزر الماوس الأيمن على القاعدة → Query Tool
4. افتح ملف `schema.sql` ونفذه
5. افتح ملف `seeds.sql` ونفذه (اختياري)

### الطريقة الثالثة: من IntelliJ IDEA

1. افتح Database Tool Window
2. أضف مصدر بيانات PostgreSQL
3. أنشئ قاعدة البيانات `project_management_db`
4. افتح `schema.sql` → انقر Run
5. افتح `seeds.sql` → انقر Run (اختياري)

## حسابات المستخدمين للاختبار

### مدير النظام
```
اسم المستخدم: admin
كلمة المرور: admin123
الدور: مدير مشروع
```

### مديرو المشاريع
```
اسم المستخدم: ahmed.ali
كلمة المرور: password123
الدور: مدير مشروع

اسم المستخدم: sara.hassan
كلمة المرور: password123
الدور: مدير مشروع

اسم المستخدم: mohammed.omar
كلمة المرور: password123
الدور: مدير مشروع
```

### أعضاء الفريق
```
اسم المستخدم: fatima.salem
كلمة المرور: password123
الدور: عضو فريق

اسم المستخدم: khalid.ibrahim
كلمة المرور: password123
الدور: عضو فريق

اسم المستخدم: nora.abdullah
كلمة المرور: password123
الدور: عضو فريق

... وغيرهم (انظر seeds.sql للقائمة الكاملة)
```

## البيانات الأولية المتوفرة

### المشاريع (6 مشاريع)
1. **تطوير تطبيق الموبايل** - قيد التنفيذ (45.5%)
2. **نظام إدارة المخزون** - قيد التنفيذ (30%)
3. **موقع التجارة الإلكترونية** - قيد التنفيذ (15.75%)
4. **تحديث البنية التحتية** - مكتمل (100%)
5. **نظام إدارة الموارد البشرية** - لم يبدأ (0%)
6. **تطبيق خدمة العملاء** - متوقف (60%)

### المهام (30+ مهمة)
- مهام مكتملة
- مهام قيد التنفيذ
- مهام قيد المراجعة
- مهام معلقة
- مهام للتنفيذ

### الإشعارات
- إشعارات غير مقروءة
- إشعارات مقروءة
- أنواع مختلفة من الإشعارات

### سجل الأنشطة
- سجل كامل لجميع العمليات المنفذة

## إعادة تعيين قاعدة البيانات

إذا أردت البدء من جديد:

```bash
# حذف القاعدة الحالية
psql -U postgres
DROP DATABASE IF EXISTS project_management_db;
CREATE DATABASE project_management_db;
\q

# إعادة تشغيل الملفات
psql -U postgres -d project_management_db -f schema.sql
psql -U postgres -d project_management_db -f seeds.sql
```

## ملاحظات مهمة

⚠️ **تحذير أمني**: 
- كلمات المرور في ملف seeds.sql غير مشفرة
- هذه البيانات للتطوير والاختبار فقط
- لا تستخدم هذه البيانات في بيئة الإنتاج
- في الإنتاج، استخدم كلمات مرور قوية ومشفرة

💡 **نصائح**:
- يمكنك تشغيل schema.sql فقط للبدء بقاعدة بيانات فارغة
- seeds.sql اختياري ويوفر بيانات للاختبار السريع
- يمكنك تعديل seeds.sql لإضافة بياناتك الخاصة

## هيكل قاعدة البيانات

### الجداول الرئيسية
- `users` - المستخدمون
- `roles` - الأدوار
- `projects` - المشاريع
- `project_status` - حالات المشروع
- `project_members` - أعضاء المشروع
- `project_roles` - أدوار أعضاء المشروع
- `tasks` - المهام
- `task_status` - حالات المهام
- `task_priority` - أولويات المهام
- `notifications` - الإشعارات
- `activity_logs` - سجل الأنشطة

### العلاقات
- كل مشروع له مدير (user)
- كل مشروع له أعضاء متعددون
- كل مهمة تنتمي لمشروع واحد
- كل مهمة مُعينة لمستخدم واحد
- كل إشعار يخص مستخدم واحد

## الدعم

إذا واجهت أي مشاكل:
1. تأكد من تشغيل PostgreSQL
2. تأكد من صحة بيانات الاتصال في `Config.java`
3. تحقق من الأخطاء في console
4. راجع ملف schema.sql للتأكد من إنشاء جميع الجداول
